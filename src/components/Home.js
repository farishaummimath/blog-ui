import React from 'react'
function Home(props){
    return (
        <div>
            <h2>Welcome to MyBlogApp</h2>
            {/* <h2>Listing Fruits - {props.fruits.length}</h2>
            <ul>
                {props.fruits.map(function(fruit,i){
                    return <li key ={i}>{fruit}</li>
                })}
            </ul> */}
        </div>
    )
}
export default Home